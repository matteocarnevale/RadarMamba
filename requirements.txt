# ============================================================
# RadarMamba — Python dependencies
# Python >= 3.9 recommended
#
# IMPORTANT — mamba-ssm requires CUDA and compiled extensions:
#   pip install mamba-ssm causal-conv1d
#   (needs CUDA >= 11.6, PyTorch >= 2.0)
#   On CPU-only machines use the pure-Python fallback in
#   src/models/mamba_core.py (slower but functional for dev)
# ============================================================

# --- Core deep learning ---
torch>=2.1.0
torchvision>=0.16.0

# --- State Space Models (Mamba) ---
# Install AFTER torch/CUDA are set up:
#   pip install mamba-ssm causal-conv1d
# Uncomment if your environment supports it:
#mamba-ssm>=1.2.0
#causal-conv1d>=1.2.0

# --- Tensor / math ---
numpy>=1.24.0
scipy>=1.11.0
einops>=0.7.0

# --- Point cloud processing ---
open3d>=0.18.0           # visualization + basic processing
scikit-learn>=1.3.0      # KDTree (scipy also works)
pypatchworkpp            # ground removal — install from source if not on PyPI:
                         #   pip install git+https://github.com/url_to_patchworkpp

# --- Config management ---
omegaconf>=2.3.0
hydra-core>=1.3.0

# --- Data I/O ---
h5py>=3.9.0              # HDF5 (RADIal uses HDF5 for "ready-to-use" data)
pandas>=2.0.0            # label CSV files
PyYAML>=6.0

# --- Visualization ---
matplotlib>=3.7.0
plotly>=5.18.0            # 3D interactive point cloud plots

# --- Training utilities ---
tqdm>=4.66.0
tensorboard>=2.14.0
wandb>=0.16.0             # optional — remove if not using W&B

# --- Testing ---
pytest>=7.4.0
pytest-cov>=4.1.0

# --- Formatting / linting ---
black>=23.0
ruff>=0.1.0
